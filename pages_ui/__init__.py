# Page modules for WorkshopIQ.
# Named `pages_ui` instead of `pages` so Streamlit's multi-page auto-discovery
# (which scans a top-level `pages/` folder) doesn't pick these up — we route
# manually from app.py.
